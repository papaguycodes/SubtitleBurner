package com.papaguycodes.subtitleburner.utils

import android.content.Context
import android.net.Uri

object FileUtils {

    fun getFileName(context: Context, uri: Uri): String {
        // Code to extract file name from Uri
        return ""
    }

    fun getOutputUri(context: Context, fileName: String, directoryUri: Uri): Uri {
        // Code to generate an output Uri based on fileName and directoryUri
        return Uri.EMPTY
    }
}
